# Open TODOs — Recipe 12.4: Lab Result Trend Analysis ⭐⭐⭐

> Auto-extracted 2026-06-18 from inline source comments (23 items). Captured before the scaffolding-cleanup pass; resolve or consciously drop each before declaring the recipe final.

## architecture — `chapter12.04-architecture.md`

- **L15** — TODO (TechWriter): Expert review N2 / L2 (LOW). Specify the EHR ingest path egress posture. The recommended posture is Direct Connect or VPN from the on-premises EHR to the AWS VPC, with the on-premises HL7 v2 listener writing to S3 via S3 gateway endpoint or HealthLake's HL7 v2 ingest API via VPC interface endpoint. For cloud-hosted EHRs use AWS PrivateLink where the vendor offers it; otherwise HTTPS webhook with mutual TLS or signed-JWT auth and TLS 1.2 minimum, with the receiving Lambda or API Gateway in the VPC. Public-internet-with-API-key alone is not appropriate for PHI-bearing inbound streams.
- **L29** — TODO (TechWriter): Expert review A1 / H2 (HIGH). Specify per-stage retry, catch-and-DLQ, and partial-failure semantics for the nightly pipeline: (1) per-stage retry on States.TaskFailed up to 3 times with exponential backoff (60s, multiplier 2.0, max 600s); (2) catch-and-route on persistent failure to a CloudWatch Logs group (PHI-scrubbed) and an SQS DLQ; (3) explicit "stale" marker write to DynamoDB on missed cycles so CDS Hooks consumers can render staleness; (4) bounded BatchWriteItem UnprocessedItems retry (5 attempts) with metric on unprocessed count; (5) CloudWatch alarms on pipeline_stage_failed > 0 in any 24-hour window (page on-call) and consecutive-day-failures > 1 (escalate to medical informatics director). Add DLQ box and stale-record write path to the architecture diagram.
- **L35** — TODO (TechWriter): Expert review A4 / M3 (MEDIUM). Specify the drift-detection and threshold-calibration pipeline: (1) a daily Lambda that computes surfaced_trends_per_clinician_per_day, surfaced_trends_per_lab_per_day, and suppressed_trends_per_lab_per_day distributions to CloudWatch; (2) a weekly Lambda that joins suppressed trends against subsequent surfaced trends and clinician feedback to produce per-LOINC threshold-calibration recommendations; (3) clinical-rule configurations stored in a versioned DynamoDB-backed config table (not Lambda env vars) so threshold adjustments deploy with a config-table write, not a Lambda redeploy; (4) CloudWatch alarms on surfaced_trends_per_clinician_per_day exceeding a configured threshold per the Honest Take's "above two per month is alarming" framing.
- **L69** — TODO (TechWriter): Expert review S1 / H1 (HIGH). Differentiate customer-managed KMS keys per data class for blast-radius containment: a CMK for raw lab feeds + harmonized data + patient baselines + HealthLake datastore (PHI), a CMK for trend scores and suppressed-trends buckets (PHI-by-association via patient_id), a CMK for clinical-rule configurations and LOINC mapping tables (operational, no PHI), a CMK for the model-artifacts bucket, a CMK for the DynamoDB serving table (PHI), a CMK for SageMaker training output, and a CMK for CloudWatch log groups. Add per-Lambda least-privilege execution roles with kms:Decrypt scoped per data class.
- **L71** — TODO (TechWriter): Expert review N1 / M8 (MEDIUM). Enumerate the full VPC endpoint set with endpoint type: gateway endpoints (free) for S3 and DynamoDB; interface endpoints (per-AZ-per-endpoint cost) for HealthLake, SageMaker (API), SageMaker (Runtime), Step Functions, EventBridge, Glue, Lambda, KMS, CloudWatch Logs, CloudWatch Monitoring, and Secrets Manager (where used for EHR-integration credentials).
- **L73** — TODO (TechWriter): Expert review S2 / M1 (MEDIUM). Specify CloudTrail data events on every PHI-bearing S3 bucket (raw feeds, harmonized data, patient baselines, trend scores, suppressed trends), the DynamoDB serving table, customer-managed KMS keys, and HealthLake (FHIR resource access logging). CloudTrail logs land in a dedicated S3 bucket with Object Lock in compliance mode and lifecycle to S3 Glacier Deep Archive after 90 days.
- **L75** — TODO (TechWriter): Expert review N3 / L3 (LOW). Note that reference-data downloads (LOINC, UCUM, Synthea, MIMIC-IV) should go through a controlled artifact-mirror (an internal S3 bucket populated by an audited download workflow) rather than direct public-internet downloads from the development VPC.
- **L77** — TODO (TechWriter): Expert review V2 / L5 (LOW). Decompose the $300-$1,500/month range by patient cohort size and monitored LOINC count. The quoted range assumes a 10,000-50,000-patient cohort with 5-10 monitored LOINC codes (typical for a CKD-and-diabetes panel). A 100,000-patient deployment with 20-30 LOINC codes can reach $2,500-$5,000/month, with HealthLake storage and SageMaker inference as the dominant cost drivers.
- **L79** — TODO (TechWriter): V1. Verify HealthLake and SageMaker pricing assumptions reflect current rates. AWS pricing changes; confirm against the AWS pricing calculator before publication.
- **L104** — TODO (TechWriter): N1. Verify all reference implementation links are still live during the pre-publication audit.
- **L110** — TODO (TechWriter): Expert review V3 / L6 (LOW). Specify time-zone discipline in harmonization: store collection_ts as a UTC instant in the harmonized record. Preserve the source-system local-time string in a separate source_local_ts field if needed for display, but all temporal ordering, baseline-window computation, and trend-duration calculation use the UTC instant. This avoids ordering bugs in multi-site health systems where labs are collected across time zones.
- **L164** — TODO (TechWriter): Expert review A2 / H3 (HIGH). Promote baseline-reset event handling from the production-gaps prose to a first-class architectural primitive: (1) a maintained baseline-reset event feed (medication starts, therapy transitions, major procedures, new diagnoses) sourced from the EHR's medication, problem, and procedure feeds; (2) baseline computation that excludes values prior to the most recent applicable reset event for each (patient, LOINC) pair, falling back to "insufficient_history" during the post-reset stabilization window; (3) a scheduled change-point detection backstop (Bayesian online change-point or PELT) that emits candidate reset events when a statistically significant level shift is detected; (4) DynamoDB record carrying baseline_reset_events metadata; (5) CloudWatch metric on baseline_resets_per_patient_per_year with anomaly alarms. This is the per-patient longitudinal-pipeline correctness primitive that separates this recipe from the volumetric forecasting recipes earlier in the chapter.
- **L315** — TODO (TechWriter): Expert review A3 / M2 (MEDIUM). Specify the idempotency contract for Step 5: (1) generated_at_ts is computed once at the pipeline-start step and propagated through Step Functions state, not recomputed per Lambda invocation; (2) add a CURRENT#<loinc_code> sort-key value per (patient_id, loinc_code) pointing to the latest surfaced trend, with a conditional write ConditionExpression: attribute_not_exists(generated_at_ts) OR generated_at_ts < :new_generated_at_ts; (3) the EventBridge trigger uses a pipeline_run_id derived from the schedule invocation ID so at-least-once delivery produces idempotent runs; (4) BatchWriteItem retry on UnprocessedItems is bounded (5 retries) and emits a metric on unprocessed count; (5) backfill writes from baseline-reset reconciliation use a revision attribute and the CURRENT conditional-write logic; (6) the suppressed-trends S3 write is keyed by (date, patient_id, loinc_code, pipeline_run_id) so reruns overwrite cleanly.
- **L317** — TODO (TechWriter): Expert review S3 / L1 (LOW). Add a sentence to "Clinical Consumers" or to the AWS Implementation noting that the surfaced-trend payload is PHI in the strongest sense (patient identifier plus dated lab values plus clinical interpretation), and every downstream consumer (CDS Hooks responder, inbox aggregator, population-health dashboard) inherits the PHI posture and must be a HIPAA-eligible service or a BAA-covered first-party application. The boundary at the DynamoDB serving table is not a PHI-to-non-PHI transition.
- **L392** — TODO (TechWriter): A1. Performance benchmarks above are typical figures for production lab trend systems running on chronic-disease panels. Confirm against your reference data sources before publication.
- **L404** — TODO (TechWriter): Expert review A6 / M5 (MEDIUM). Promote medication-and-intervention awareness from production-gaps prose to an architectural primitive: (1) a maintained feed from EHR medication and procedure systems, ingested via FHIR MedicationRequest and Procedure resources into HealthLake; (2) a per-LOINC configuration that maps LOINC codes to relevant medications and procedures (e.g., creatinine to ACE inhibitor, ARB, contrast agent; hemoglobin to ESA, iron supplementation, transfusion); (3) the trend-detection step optionally takes the medication-and-intervention context as a feature input; (4) the clinical-relevance step optionally suppresses or down-weights trends temporally adjacent to a relevant medication start or procedure; (5) the surfaced-trend payload includes the medication-and-intervention context that was considered, so the clinician sees the system is aware of e.g. the ACE inhibitor start three months ago. Per the recipe's own framing, this is the single most important production refinement.
- **L408** — TODO (TechWriter): Expert review A7 / M6 (MEDIUM). Add an optional "Panel-Level Aggregation" architectural step between the per-LOINC clinical-relevance step and the clinical-consumers delivery: a Lambda that consumes per-LOINC surfaced trends, groups them by clinically-defined panels (kidney function = creatinine + eGFR + BUN + electrolytes; liver function = AST + ALT + ALP + bilirubin), and applies panel-level rules. The architecture should show this as an optional step that defaults off in the basic pipeline and can be enabled per panel as the deployment matures.
- **L414** — TODO (TechWriter): Expert review A8 / M7 (MEDIUM). Add a CDS Hooks responder to the architecture (API Gateway + Lambda, both VPC-deployed and behind WAF) wired to the DynamoDB serving table. The Lambda implements the CDS Hooks specification, queries DynamoDB by patient_id on the patient-view hook, formats the surfaced trends as CDS Hooks cards, and returns them with appropriate severity-band styling. Show this path in the architecture diagram; the EHR's CDS Hooks client connects to the API Gateway endpoint over a HIPAA-eligible mutual-TLS or signed-JWT channel.
- **L420** — TODO (TechWriter): Expert review A2 / H3 (HIGH). See also the Step 2 marker. This bullet identifies the failure mode; the architecture in the AWS Implementation section should be updated with the explicit baseline-reset event feed, the post-reset stabilization window, and the change-point backstop.
- **L424** — TODO (TechWriter): Expert review A5 / M4 (MEDIUM). Promote harmonization quality assurance from production-gaps prose to an architectural primitive: (1) an unmapped-tests SQS queue and S3 quarantine bucket where harmonization failures land for manual review, with an SNS topic to the data-engineering team; (2) a daily Glue job that computes per-LOINC value-distribution statistics (mean, median, IQR, fraction outside the lab's reference range) and compares them against a rolling baseline; (3) CloudWatch alarms on per-LOINC distribution shift exceeding a configured threshold; (4) a backfill workflow that, when a harmonization fix is deployed, re-runs harmonization on affected historical records and triggers downstream baseline and trend recomputation with a revision attribute for audit trail.
- **L430** — TODO (TechWriter): Expert review V4 / L7 (LOW). Connect the regulatory framing to the explanation_text construction: the explanation_text field constructed in Step 4 is the architectural primitive that satisfies the Cures Act CDS-exemption transparency-and-explainability requirement. The clinician must be able to independently review the basis for the surfaced trend (slope, duration, recent values, baseline, lab's reference range), which the explanation_text and recent_values payload deliver. This is why the construction is deterministic in pseudocode rather than LLM-generated; an LLM-generated explanation that hallucinates the underlying numbers is a regulatory exposure.
- **L463** — TODO (TechWriter): R1. Search aws-samples and aws-solutions-library-samples for current HealthLake or FHIR samples and add 1-2 specific repositories to this section before publication.
- **L478** — TODO (TechWriter): N3. Audit all external links during the final pre-publication pass. LOINC, UCUM, Synthea, MIMIC-IV, Hyndman textbook, CDS Hooks, and the Adams & MacKay paper are stable. AWS doc and blog links should be re-verified.
